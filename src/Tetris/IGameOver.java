package Tetris;

/**
 * Game over interface. Implements the Command Pattern.
 */
public interface IGameOver {
    /**
     * Does something when a game over is triggered.
     */
    public void exec();
}
